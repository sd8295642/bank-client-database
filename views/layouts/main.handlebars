<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Amityville Bank Clientele Database</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Frijole&family=MedievalSharp&family=Pathway+Gothic+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class = "theme">
    
    <div class="flex-column justify-center wrap">
      <header class="display-flex justify-space-between align-center p-4 header">
        <h1>
          <a class="header-title" href="/">Amityville Bank Clientele Database</a>
        </h1>
        <nav class="nav">
          {{#if logged_in}}
            <button class="no-button" id="logout">Logout</button>
            
          {{else}}
            <a class="login-link" href="/login">Login</a>
            
          {{/if}}
        </nav>
      </header>
      <main class="container container-fluid mt-5 main">
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}

  </body>
</html>